- var average = review.average
div.Post
  div.Title
    h2 Service: #{review.service}
    h2 Title: #{review.title}
    h2 Average Rating: #{average}
    //- overall only for preview. 
    h2 Content:
    p #{review.text}
    button Comment
    button Edit
    //- button.vote-post-button(type="submit" onClick='voteFormHandler(' + review.id + ', ' + user.id + ')') Vote
//- script(src="/javascript/vote.js")
    